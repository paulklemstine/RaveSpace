{
  "name": "ravespace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "post-deploy": "tsx scripts/post-deploy.ts",
    "deploy": "npm run build && npm run post-deploy && firebase deploy --only hosting"
  },
  "dependencies": {
    "gl-transitions": "^1.43.0",
    "meyda": "^5.6.0",
    "peerjs": "^1.5.5",
    "realtime-bpm-analyzer": "^5.0.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/three": "^0.170.0",
    "tailwindcss": "^4.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-glsl": "^1.3.0",
    "vitest": "^2.1.0"
  }
}
